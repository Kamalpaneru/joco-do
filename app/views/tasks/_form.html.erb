<%= simple_form_for(@task) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :content %>
    <%= f.input :state, :collection => %w[Mike Nick Jason Tim] %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
